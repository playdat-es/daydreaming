NODE_ENV=development
PORT=3001

# Comma-separated allowlist. Empty = allow all origins.
CORS_ORIGINS=

# Neon pooled connection string. Leave empty for local dev — the server
# falls back to an embedded PGlite instance persisted to ./.pglite/.
# Required in production.
DATABASE_URL=
